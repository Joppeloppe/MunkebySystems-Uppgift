asp .net hemsida 
	logga in - inlogg kryperat i databas
	hämta data från databas med sql syntax

======================================================
	

två tabeller - en account + kryperat lösenord 
					- en data
					
	vid inloggning - hämta account med samma användarnamn, 
								hasha lösen från hemsida sen jämför mot databas
	
hemsida
	login
	restauranger(namn) - klick -> visa mer information
	visa mer information - chart
	
	
	
create database MUNKABY_SYSTEMS
create table user(id bigint, username varchar(50), password varchar(MAX));
create table population_'NAME'(year int, population bigint)

insert into user(username, password)values(ID #största + 1(find och sort på ID ta #1)#, 'USERNAME', 'PASSWORD')


==========================

run Microsoft SQL Server Management Studio as Administrator
servername : DESKTOP-6NKSGDG\MSSQLSERVER01
connectionstring : Server=localhost\MSSQLSERVER01;Database=MunkebySystemsDatabase;Trusted_Connection=True;